[pytest]
markers =
    external: marks tests that depend on external services (network access required)
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestUnknownMarkWarning
    # You might add more specific filters if needed